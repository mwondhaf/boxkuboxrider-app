import MapScreen from "@/components/common/Map";
import { View } from "react-native";

const HomeScreen = () => {
  return (
    <View style={{ flex: 1 }}>
      <MapScreen />
    </View>
  );
};

export default HomeScreen;
